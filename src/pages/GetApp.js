import React from 'react';

const GetApp = () => {
  return (
    <section
      id="getApp"
      className="h-screen flex items-center justify-center text-center bg-blue-100"
      data-scroll-section
    >
    <div>
    <h1>Scan to download/link</h1>
    <h4>Get the Adapt app now and let’s take you towards your personalised wellness path!</h4>
    </div>
    </section>
  );
};

export default GetApp;
